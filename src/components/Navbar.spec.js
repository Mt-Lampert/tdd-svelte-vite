/*
import "@testing-library/jest-dom";
import {
  findByTestId,
  findByText,
  render,
  screen,
} from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import { vi } from "vitest";
import App from "./App.svelte";
*/

describe("Navbar", () => {
  it.todo("has a link to the Landing page", () => {
    // >> Test like this:
    // queryXXX can also return 'null'
    // const link = screen.queryByTestId("home-link")
    // ...
    // expect(link.getAttribute("href")).toBe("/");
  });

  it.todo("has a link to the Signup page");

  it.todo("has a link to the Login page");

  it.todo("has a link to the users page");
})